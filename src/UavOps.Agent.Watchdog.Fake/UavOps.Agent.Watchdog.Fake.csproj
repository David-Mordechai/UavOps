<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Bumped from 8.0.2: UavOps.Agent.Contracts now transitively requires a much newer floor via
         its own ModelContextProtocol package reference (added for McpToolsBuilder) - NuGet treats
         this project's own lower explicit pin as a downgrade otherwise. -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.10" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\UavOps.Agent.Contracts\UavOps.Agent.Contracts.csproj" />
  </ItemGroup>

</Project>
